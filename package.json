{
  "license": "MIT",
  "scripts": {
    "start": "ts-node index.ts",
    "lido": "yarn start lido",
    "scripts": "yarn start scripts",
    "token-manager": "yarn start token-manager",
    "voting": "yarn start voting",
    "accounting-oracle": "yarn start accounting-oracle",
    "exit-bus-oracle": "yarn start exit-bus-oracle"
  },
  "devDependencies": {
    "@types/node": "^18.13.0",
    "dotenv": "^16.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "commander": "^10.0.0",
    "ethers": "^6.0.2"
  }
}
