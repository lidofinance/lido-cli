{
  "license": "MIT",
  "scripts": {
    "start": "ts-node index",
    "accounting-oracle": "ts-node index accounting-oracle",
    "acl": "ts-node index acl",
    "exit-bus-oracle": "ts-node index exit-bus-oracle",
    "lido": "ts-node index lido",
    "nor": "ts-node index nor",
    "scripts": "ts-node index scripts",
    "staking-router": "ts-node index staking-router",
    "token-manager": "ts-node index token-manager",
    "voting": "ts-node index voting"
  },
  "devDependencies": {
    "@types/node": "^18.13.0",
    "dotenv": "^16.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "commander": "^10.0.0",
    "ethers": "^6.0.2"
  }
}
